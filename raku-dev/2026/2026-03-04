[08:42] *** sivoais left
[11:02] *** finanalyst joined
[11:14] *** finanalyst left
[12:00] <lizmat> this feels LTA:

[12:00] <lizmat> m: 56792.chr

[12:00] <camelia> rakudo-moar af30c7bed: ( no output )

[12:00] <lizmat> m: say 56792.chr

[12:00] <camelia> rakudo-moar af30c7bed: OUTPUT: «Error encoding UTF-8 string: could not encode Unicode Surrogate codepoint 56792 (0xDDD8)␤  in block <unit> at <tmp> line 1␤␤»

[12:01] <lizmat> ShimmerFairy ^^ 

[12:02] <lizmat> it's nqp::encoderepconf() throwing in Encoding::Encoder::Builtin.encode-chars

[13:33] <timo> really depends on whether we want Str in NFG to be able to hold a lone surrogate codepoint or not

[13:34] <timo> it makes sense that it explodes when trying to encode it to a buf in order to print it out

[13:34] <timo> m: 56792.chr.encode("utf8-c8").say

[13:34] <camelia> rakudo-moar af30c7bed: OUTPUT: «Error encoding UTF-8 string: could not encode Unicode Surrogate codepoint 56792 (0xDDD8)␤  in block <unit> at <tmp> line 1␤␤»

[13:34] <timo> i guess that's not how you get that out of there huh

[13:40] <timo> https://gist.github.com/milseman/c22a0413dee131719fd51407e3ee7c8b - surely interesting to cross-reference

[13:45] <[Coke]> m: 56792.chr.NFKD.say

[13:45] <camelia> rakudo-moar af30c7bed: OUTPUT: «NFKD:0x<ddd8>␤»

[16:32] *** sivoais joined
[16:48] *** [Coke]_ joined
[16:51] *** [Coke] left
[18:38] *** [Coke]_ is now known as [Coke]

[19:30] <Geth> ¦ rakudo/main: a97c7a33c1 | (Elizabeth Mattijsen)++ | src/core.c/IO/Path.rakumod

[19:30] <Geth> ¦ rakudo/main: Rework IO::Path.slurp(:bin)

[19:30] <Geth> ¦ rakudo/main: 

[19:30] <Geth> ¦ rakudo/main: Inspired by af30c7bed30b725a12

[19:30] <Geth> ¦ rakudo/main: 

[19:30] <Geth> ¦ rakudo/main: Instead of *always* asking for the filesize beforehand, it now asks

[19:30] <Geth> ¦ rakudo/main: for the filesize if the initial read filled the initial size buffer

[19:30] <Geth> ¦ rakudo/main: (1MB).  If that exceeds INT_MAX (minus 1MB) a slow path is taken

[19:30] <Geth> ¦ rakudo/main: <…commit message has 9 more lines…>

[19:30] <Geth> ¦ rakudo/main: review: https://github.com/rakudo/rakudo/commit/a97c7a33c1

[19:32] <lizmat> m: use nqp; my $b = nqp::setelems(Buf.new,0x080000000).decode

[19:32] <camelia> rakudo-moar af30c7bed: OUTPUT: «MoarVM panic: Memory allocation failed; could not allocate 2147483648 bytes␤»

[19:32] <lizmat> m: use nqp; my $b = nqp::setelems(Buf.new,0x07fffffff).decode

[19:32] <camelia> rakudo-moar af30c7bed: OUTPUT: «MoarVM panic: Memory allocation failed; could not allocate 2147483647 bytes␤»

[19:32] <lizmat> m: use nqp; my $b = nqp::setelems(Buf.new,0x07ffffff).decode

[19:33] <camelia> rakudo-moar af30c7bed: ( no output )

[19:33] <lizmat> m: use nqp; say nqp::setelems(Buf.new,0x07ffffff).decode.chars

[19:33] <camelia> rakudo-moar af30c7bed: OUTPUT: «134217727␤»

[19:33] <lizmat> m: use nqp; say nqp::setelems(Buf.new,0x07ffffff).decode.chars.base(16)

[19:33] <camelia> rakudo-moar af30c7bed: OUTPUT: «7FFFFFF␤»

[19:34] <lizmat> m: use nqp; say nqp::setelems(Buf.new,0x07fffffff).decode.chars.base(16)

[19:34] <camelia> rakudo-moar af30c7bed: OUTPUT: «MoarVM panic: Memory allocation failed; could not allocate 2147483647 bytes␤»

[19:34] <lizmat> weird, that works on my machine, but then again that has 64G

[19:35] <lizmat> 0x07fffffff works for me, 0x08000000 fails

[19:35] <lizmat> so I guess I will put in a check for > 0x07fffffff before trying to decode

[19:51] <timo> PIO? oh yeah that's uhhh Physical Input/Output of course :)

[20:27] *** patrickb left
[20:41] *** patrickb joined
[20:47] *** finanalyst joined
[21:03] <Geth> ¦ rakudo/main: 06f16f6d58 | (Elizabeth Mattijsen)++ | 2 files

[21:03] <Geth> ¦ rakudo/main: Provide better error when trying to decode too large blobs

[21:03] <Geth> ¦ rakudo/main: 

[21:03] <Geth> ¦ rakudo/main: Apparently at least on some OSes (and/or on MoarVM) it's impossible

[21:03] <Geth> ¦ rakudo/main: to decode buffers that have more than 0x07fffffff elements: this

[21:03] <Geth> ¦ rakudo/main: used to throw a hard untrappable memory exceeded error thrown from

[21:03] <Geth> ¦ rakudo/main: the guts of the VM.

[21:03] <Geth> ¦ rakudo/main: 

[21:03] <Geth> ¦ rakudo/main: This adds logic to check the number of elements in the blob to be

[21:03] <Geth> ¦ rakudo/main: decoded, and produces a Failure if the number of elements is too large:

[21:03] <Geth> ¦ rakudo/main: 

[21:03] <Geth> ¦ rakudo/main:   Too many bytes to decode: 2418709326 is more than 2147483647

[21:03] <Geth> ¦ rakudo/main: review: https://github.com/rakudo/rakudo/commit/06f16f6d58

[21:56] <[Coke]> ;win 12

[22:06] *** finanalyst left
[23:36] <ShimmerFairy> lizmat: To respond to earlier, surrogates are where things get real subtle. Surrogate codepoints are, in fact, codepoints, and you can manipulate them just like you would any other codepoint (though, obviously, their practical use is quite limited). Surrogates however are not "Unicode scalar values", which is the only kind of codepoint allowed in the UTF encodings. So trying to store a surrogate in any UTF encoding is an error, but 

[23:36] <ShimmerFairy> holding onto one as a codepoint isn't. Since NFG strings operate at a higher level than raw UTF encoding, there's a logic to them being OK in Strs.

[23:40] <ShimmerFairy> I tried looking, but Unicode doesn't seem to have guidance on how a language's string type should handle surrogate codepoints, and I'm not sure what I think the right answer is. For one thing, since Strs are conceptualized as grapheme sequences (i.e. codepoint sequences with bookkeeping), it makes sense to allow all valid codepoints in them. On the other hand, you'll never be able to do UTF-based I/O with such strings, so their 

[23:40] <ShimmerFairy> usability is limited to within the program, and I can't offhand think of any uses.

[23:44] <timo> put it in a string you *really* don't want your program to output so users or other processes can see them :) :)

[23:47] <ShimmerFairy> Yeah, you could (ab)use them as like noncharacters that absolutely must not exit the program's internal memory.


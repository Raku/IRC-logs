[11:20] *** librasteve_ joined
[15:27] *** david32 joined
[16:51] *** david32 left
[20:32] <Geth> ¦ GD-Raw: jforget++ created pull request #2: Implementing gdImageEllipse and gdImageRectangle.

[20:32] <Geth> ¦ GD-Raw: review: https://github.com/raku-community-modules/GD-Raw/pull/2

[22:11] *** david7832 joined
[23:04] <david7832> @lizmat: I've used your paths module today, feeding its outputs into a shell command like this: qqx⟨crc32 "$^f"⟩.  Some of the filenames did contain a double quote, so I had to escape them first:

[23:04] <david7832>     for paths».subst('"', '\"') { qqx⟨crc32 "$^f" ⟩

[23:04] <david7832> In fact, I should have escaped all four of  <" \ ` $>

[23:04] <david7832> Would you consider merging a PR that adds an option to do this automatically?

[23:04] <david7832> I'm thinking about extending the signature like this:

[23:04] <david7832>     sub paths( ...., :dqescape(:$doublequoteescape))

[23:04] <david7832> To be used like:

[23:04] <david7832>     for paths('/home/ds'):dqescape { qqx⟨some-command "$^f⟩ }

[23:04] <david7832> The reason behind this is because I think this use case is pretty common: One wants to apply shell commands to the paths and thus has to properly escape them. In bash and sh at least, double quotes are the sanest way for this, as far as I'm aware.

[23:04] <david7832> (Can't say for sure if I'll actually make such a PR, since it looks like I'll have to learn nqp first, but that would give me a decent goal to learn towards :) )

[23:06] <lizmat> then I suggest something like :shell        so we can make it depending on $*SPEC

[23:09] <david7832> good point. I suspect it's very desirable to be explicit about the quoting method; perhaps there is a solution to do both

[23:11] <lizmat> otoh, it feels to me that paths should be kept as simple and fast as possible

[23:12] <lizmat> perhaps the documentation should be enhanced...

[23:12] <lizmat> also I wonder if the IO::Spec modules contain logic for escaping?

[23:18] <david7832> regarding paths: perhaps there's a way to add the option in such a way that it only really impacts the performance when it's used.

[23:18] <david7832> I can definitely see why it's good to keep that module simple. However (even though on Unix we're in "you asked for it, you get it" territory) I think the fact that a builtin quoting option also protects users against accidental shell havoc carries some weight

[23:19] <david7832> since I think the fact is that module will be used for insertion into shell commands quite often :)

[23:20] <david7832> but I'll definitely look into IO::Spec, perhaps it is the best place for escaping logic after all

[23:28] <lizmat> don't worry about performance much: a named argument can be handled by another candidate, and thus already create a slow(er) path in that case without affecting the one without the named arg

[23:34] <david7832> Ah, very good then

[23:34] <david7832> As for the signature, how about:

[23:34] <david7832> for paths:escapefor("'")

[23:34] <david7832> for paths:escapefor('single quote')

[23:34] <david7832> i.e. when calling it, one indicates the construct inside of which one intends to use the paths, and the actual quoting behavior then depends on $*SPEC (and potentially other system variables)

[23:36] <lizmat> if you want to make it convenient for shell escaping, it should do *all* the escapes for that shell

[23:37] <lizmat> anyways, I'll see what you come up with after some shuteye  :)

[23:37] <lizmat> afk&

[23:37] <david7832> thanks for all the infos, good night!

[23:50] *** librasteve_ left

[01:51] *** Ven joined
[02:56] *** ilbot3 joined
[11:02] *** Ven joined
[11:25] <Ven``> Started a bit working on captures, and now I'm like <I have no idea what I'm doing>
[11:25] <Ven``> I... guess I want to introduce a Match object.
[11:25] <Ven``> because Perl 6 relies on that for its nested matching groups goodness. As well as named groups.
[14:25] <masak> I can see how we could use a Match object
[14:26] <masak> question is more, do we stick to Perl 6's model of Match objects, or Python's?
[14:27] <masak> hm. maybe neither
[14:30] <masak> `<EXPR>` in the regex is problematic, since Perl 6 regexes would make that a named capture, but the (still negotiable) implicit capture<->parameter binding is strictly positional

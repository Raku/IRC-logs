[01:49] *** ilbot3 joined
[15:33] <masak> I have this lovely notion where `next`, `last`, and `redo` could be implemented as third-party language extensions on top of some "loop provider" object
[15:34] <masak> built-in `for` and `while` loops would already publish such a loop provider
[15:34] <masak> and if you created a new loop form, it'd do the same
[15:34] <masak> basically, I would want as many aspects of 007 as possible to work like this
[15:34] <masak> signature binding would be another kind of provider
[15:35] <masak> to allow for things like optional parameters, defaults, slurpies
[21:10] *** Ven joined

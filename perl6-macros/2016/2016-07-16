[01:48] *** ilbot3 joined
[04:43] <masak> today's "devil's advocate" idea: comma in term position is actually free, so we could still use it for unquoting
[04:44] <masak> or ,() or some such
[04:44] <masak> but it's missing the cute symmetry with ` and `() that Lisp quasiquoting has
[04:45] <masak> and (as I type this I realize) that the main reason Lisp gets away with a one-sided quasiquotation marker and a one-sided unquote marker is that it puts parentheses around all its expressions by default
[04:46] <masak> Perl 6 and 007 don't, and so don't get away with the same
[04:46] <masak> maybe that's patently obvious, but it hadn't really occurred to me before
[04:47] <masak> in other happy news: I'm about halfway through "Let Over Lambda" -- currently reading about anaphoric macros
[08:19] *** FROGGS joined
[09:01] *** Ven joined
[14:30] <Ven> well, ,() might technically be free, but it's only interesting for unquoting in term position
[14:55] *** Ven joined
[15:12] *** Ven joined
[15:26] *** Ven joined

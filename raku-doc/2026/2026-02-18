[00:04] *** librasteve_ left
[04:25] <Geth> ¦ doc/main: 1ff280b863 | 2colours++ | 2 files

[04:25] <Geth> ¦ doc/main: Add and reference new `."" operator` entry

[04:25] <Geth> ¦ doc/main: review: https://github.com/Raku/doc/commit/1ff280b863

[04:25] <Geth> ¦ doc/main: f9eef345cd | (Eric Forste)++ (committed using GitHub Web editor) | 2 files

[04:25] <Geth> ¦ doc/main: Merge pull request #4769 from 2colours/runtime-method-names

[04:25] <Geth> ¦ doc/main: 

[04:25] <Geth> ¦ doc/main: Add and reference new `."" operator` entry

[04:25] <Geth> ¦ doc/main: review: https://github.com/Raku/doc/commit/f9eef345cd

[12:15] <lizmat> https://docs.raku.org/routine/is%20rw   doesn't mention containers, I think it should as there is a whole page explaining containers in Raku: https://docs.raku.org/language/containers

[12:16] <lizmat> also I think the example there is too complicated: something like:

[12:16] <lizmat> my $a; sub foo() is rw { $a }; foo() = 42; say $a

[12:16] <lizmat> should really be enough to get the point across

[12:33] <lizmat> also, the statement "When a routine is modified with this trait, its return value will be writable." is incorrect

[12:33] <lizmat> m: sub a() is rw { 42 }; a() = 666

[12:34] <camelia> rakudo-moar a3566826f: OUTPUT: «Cannot modify an immutable Int (42)␤  in block <unit> at <tmp> line 1␤␤»

[12:34] <lizmat> so I think  the language should be more container centric, and mention that "is rw" will not de-containerize

[13:19] <lizmat> another (maybe known issue): https://docs.raku.org/syntax/DWIM shows a link to WAT

[13:20] <lizmat> however, the link as a link to an anchor on the same page, rather than to: https://docs.raku.org/syntax/WAT   (and vice-versa)

[13:21] <lizmat> so theses generated pages would need to have their links inlocalized?

[13:35] <[Coke]> That bug sounds familiar.

[13:40] <[Coke]> but I don't see it on doc-website, where I'd expect it.

[13:49] <[Coke]> lizmat: https://github.com/Raku/doc-website/issues/483

[14:07] *** samebchase2 joined
[14:07] <disbot13> <nemokosch> returning a non-writable value from an is rw sub seems like a DIHWIDT at best

[14:09] *** samebchase left
[14:09] *** samebchase2 is now known as samebchase

[14:09] <disbot13> <nemokosch> sub modify($a is rw) { $a = 42; }; modify(53); # this throws without ever executing the body

[14:11] <disbot13> <nemokosch> as things are, is there even a difference between is rw and is raw for return values?

[14:16] <disbot13> <nemokosch> the current code example is a bit scary for sure

[16:59] *** finanalyst_ joined
[21:05] *** tbrowder left
[21:05] *** tbrowder joined
[22:19] *** finanalyst_ left

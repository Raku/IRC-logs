[01:10] *** TimToady joined
[01:49] *** ilbot3 joined
[01:49] *** moderator changes topic to: Fire is step THREE! | https://github.com/perl6/toolchain-bikeshed | Channel logs: http://irclog.perlgeek.de/perl6-toolchain/today | useful prior art: https://metacpan.org/pod/CPAN::Meta::Spec
[07:56] <nine> ugexe: oh, that's indeed a clear oversight!
[07:58] <nine> ugexe: CUR::AbsolutePath is really just for require "/foo/bar.pm"; This used to be handled by CUR::FileSystem but that required at least one of those in the repo chain and it muddled responsibilities.
[08:10] *** domidumont joined
[08:10] *** domidumont joined
[08:15] *** domidumont joined
[09:21] *** camelia joined
[10:01] *** FROGGS joined
[13:50] *** b2gills joined
[16:58] *** domidumont joined
[17:08] <ugexe> Is there any way to add debug output to CU::PS::File? If I add `note`s the output works during `make install` but gets stuck here (https://github.com/rakudo/rakudo/blob/7a98505208fbdb65af93caa0026246864396ce5c/src/core/CompUnit/PrecompilationRepository.pm#L219) when running zef (while precompiling the first module)
[17:38] <ugexe> if i remove :err from the call to `run` and comment out the 2 instances of $proc.err in CU::PR then it does not get stuck
[17:45] *** stevieb joined
[17:52] *** stevieb joined
[18:00] <ugexe> that last one is a bug with run
[18:13] <ugexe> Here are the changes i've come up with so far: https://github.com/rakudo/rakudo/compare/nom...ugexe:cur-updates?expand=1
[18:15] <ugexe> $proc.err cant be `slurp-rest`ed (forking error), hence the $proc.err.lines.join("\\n")
[18:16] <ugexe> but $proc.err has to have it contents read to avoid getting stuck at where i mentioned earlier
[18:26] <ugexe> that changes the `require <Build.pm>` error message from "Failed" to the better "No such symbol 'Build'"
[19:06] *** edehont joined

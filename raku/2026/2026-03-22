[01:39] *** jgaz left
[02:55] *** hulk joined
[02:57] *** kylese left
[03:15] *** hulk left
[03:15] *** kylese joined
[04:25] *** Aedil joined
[06:28] <disbot2> <melezhik.> o/

[06:29] <disbot2> <melezhik.> I am  hitting malformed UTF-8 exception when handling process stout in this line of code - https://github.com/melezhik/Sparrow6/blob/da19ca5855bda2d3ac286285ea7c20c6c43cd80b/lib/Sparrow6/Task/Runner/Helpers/Common.rakumod#L143

[06:30] <disbot2> <melezhik.> I have googled the issue , tried few workarounds,  nothing helped

[06:31] <disbot2> <melezhik.> For example I tried pass :out, :bin to Proc.new

[06:33] <disbot2> <melezhik.> I ended up piping command stdout ( before passing back to Raku ) to  iconv -f UTF-8 -t UTF-8 -c ( -c silently drops none UTF symbols ) but this is a bit ugly as iconv side effect if buffering which I could not overcome

[06:34] <disbot2> <melezhik.> Any ideas ?

[06:36] <disbot2> <melezhik.> https://gist.github.com/melezhik/bac2eef77560f7aa183e441f1660d9b0

[06:37] <disbot2> <melezhik.> Full log

[06:41] <disbot2> <melezhik.> The issue occurs on the latest Rakudo version at least

[06:52] <disbot2> <melezhik.> https://github.com/rakudo/rakudo/issues/6092

[08:26] *** annamalai left
[08:34] *** Sgeo left
[08:35] <disbot2> <melezhik.> The context : external command producing output passing to Raku is qemu, I run qemu vm which is bootstrapped via kickstart , I guess most of the output is valid text data , but a sone point it may have chucks of bytes / binary / none utf8

[08:42] *** annamalai joined
[09:39] *** abraxxa joined
[09:39] *** abraxxa left
[09:39] *** abraxxa joined
[09:43] *** abraxxa left
[10:21] <lizmat> try using utf8-c8 as encoding

[11:08] *** sp1983 joined
[11:08] <sp1983> liz: do you mean this? - https://gist.github.com/melezhik/bac2eef77560f7aa183e441f1660d9b0?permalink_comment_id=6044875#gistcomment-6044875

[11:09] <lizmat> if Proc::Async.new takes an :enc argument, yes

[11:09] <sp1983> ok, this leads to this error during installation - https://gist.github.com/melezhik/bac2eef77560f7aa183e441f1660d9b0?permalink_comment_id=6044876#gistcomment-6044876

[11:11] <sp1983> hrm , probably the parameter should go first - $proc = Proc::Async.new(:enc<utf8-c8>,@arr);

[11:12] <sp1983> let me try

[11:12] <sp1983> at least this installs without errors

[11:14] <sp1983> so :enc<utf8-c8> should not fall, even though data contain none UTF8 , right ? in my case I can't even control what goes from `qemu` bootstrap process stdout ...

[11:19] <lizmat> looks like Proc::Async.new does *not* take a :enc argument

[11:19] <lizmat> but yeah, that's the idea

[11:22] <sp1983> does not it? at lease this successfully installed and runs:

[11:22] <sp1983> https://gist.github.com/melezhik/bac2eef77560f7aa183e441f1660d9b0?permalink_comment_id=6044884#gistcomment-6044884

[11:23] <lizmat> then the doc is incomplete

[11:25] <sp1983> ok, do I pass enc utf-c then to ProcAync then? or to it's methods in use ...?

[11:25] <sp1983> do I -> how do I

[11:27] *** sp1983 left
[11:29] *** sp1983 joined
[11:42] *** sp1983_ joined
[11:42] <sp1983_> oh, you mean then `$proc = Proc::Async.new(:enc<utf8-c8>,@arr);` is liable ?

[11:43] <sp1983_> just not documented ? 

[11:43] *** sp1983_ left
[11:44] <sp1983> liable -> correct 

[11:46] <lizmat> yeah

[11:48] <sp1983> ok

[12:01] *** sp1983 left
[12:41] *** sibl joined
[13:08] *** sibl left
[14:32] *** sp1983 joined
[14:33] *** sp1983 left
[14:33] *** sp1983 joined
[14:33] <sp1983> official documentation on ProcAsync.new reads that "multi method new(*@ ($path, *@args), :$w, :$enc, :$translate-nl, :$arg0,                  :$win-verbatim-args = False, :$pty,                  :$started = False --> Proc::Async:D)"

[14:34] <sp1983> maybe this is a bug? as :$w, :$enc should go before `*@ ($path, *@args)`?

[14:43] *** sp1983 left
[14:48] <ugexe> seems easy enough to test no?

[14:48] <ugexe> m: sub foo(*@ ($foo, *@bars), :$xxx) { say $xxx }; foo(1,2,3, :xxx(42))

[14:48] <camelia> rakudo-moar 7bb739181: OUTPUT: «42␤»

[14:48] <ugexe> m: sub foo(:$xxx, *@ ($foo, *@bars)) { say $xxx }; foo(1,2,3, :xxx(42))

[14:48] <camelia> rakudo-moar 7bb739181: OUTPUT: «42␤»

[14:51] <ugexe> regarding your actual issue, e.g. `whenever $proc.stdout(:enc<...>) { }` should work

[15:09] *** sp1983 joined
[15:14] *** silug left
[15:14] *** silug joined
[15:15] *** silug left
[15:16] *** silug joined
[15:16] *** sp1983 left
[16:46] *** Sgeo joined
[17:10] <Voldenet> lizmat: Proc::Async says that enc is supported in https://docs.raku.org/type/Proc/Async#method_new

[17:13] <Voldenet> m: my $proc = Proc::Async.new(<cat /dev/urandom>, :out, :enc("utf8")); react { whenever $proc.stdout.lines() { $proc.kill(SIGKILL) if $++ ~~ 100 }; whenever $proc.start { } }

[17:13] <camelia> rakudo-moar 74da84b05: OUTPUT: «A react block:␤  in block <unit> at <tmp> line 1␤␤Died because of the exception:␤    Malformed UTF-8 near bytes 31 4e f5␤␤»

[17:13] <Voldenet> m: my $proc = Proc::Async.new(<cat /dev/urandom>, :out, :enc("utf8-c8")); react { whenever $proc.stdout.lines() { $proc.kill(SIGKILL) if $++ ~~ 100 }; whenever $proc.start { } }

[17:13] <camelia> rakudo-moar 74da84b05: ( no output )

[17:14] <Voldenet> so I believe using :enc will work

[17:23] <Voldenet> the alternative could be using binary stdout entirely

[17:23] <Voldenet> m: my $proc = Proc::Async.new(<cat /dev/urandom>); react { whenever $proc.stdout(:bin) { .say; $proc.kill(SIGKILL) if $++ ~~ 100 }; whenever $proc.start { } }

[17:23] <camelia> rakudo-moar 74da84b05: OUTPUT: «Buf[uint8]:0x<20 1E 22 37 8A 13 40 51 F5 48 4C 7D 30 C2 E7 50 2D 2C 74 15 0A A3 29 15 3A A9 C2 9C 5C 63 60 99 9C 4F EA 2A 12 61 F8 36 2E 9C FC D2 3C AA DE 5F E7 84 EF 15 D2 5D 68 DB E8 D5 39 1D 1E 59 E2 62 55 5A BE B8 B7 7A 13 1C 07 C6 B4 F7 98 DB 2D …»

[17:24] <Voldenet> you get Bufs, that way, not lines

[17:24] <Voldenet> though in case of CI this may be even useful to preserve binary output and render it into proper encoding on the fly

[17:29] <Voldenet> m: my $proc = Proc::Async.new(<cat /dev/urandom>); react { whenever $proc.stdout(:bin) { .decode("utf8-c8").ords.elems.say; .decode("windows1252").ords.elems.say; $proc.kill(SIGKILL) if $++ ~~ 5 }; whenever $proc.start { } }

[17:29] <camelia> rakudo-moar 74da84b05: OUTPUT: «(timeout)287␤128␤558␤256␤1144␤512␤2408␤1024␤4745␤2048␤9525␤4096␤18870␤8192␤37680␤16384␤75681␤32768␤150513␤65536␤253224␤109760␤49147␤21440␤75793␤32768␤93290␤40320␤83746␤36544␤49101␤21440␤7…»

[17:39] <Voldenet> in fact, js could probably decode that binary better, because it can handle more encoding variants than raku at the moment

[17:40] <Voldenet> latin-2 for example

[18:18] *** david7832 joined
[18:28] *** sp1983 joined
[18:31] <sp1983> : my $proc = Proc::Async.new(:enc<utf8-c8>, <cat /dev/urandom>,); react { whenever $proc.stdout.lines() { $proc.kill(SIGKILL) if $++ ~~ 100 }; whenever $proc.start { } }

[18:32] <sp1983> m: my $proc = Proc::Async.new(:enc<utf8-c8>, <cat /dev/urandom>,); react { whenever $proc.stdout.lines() { $proc.kill(SIGKILL) if $++ ~~ 100 }; whenever $proc.start { } }

[18:32] <camelia> rakudo-moar 74da84b05: ( no output )

[18:32] <sp1983> my $proc = Proc::Async.new(:enc<utf8>, <cat /dev/urandom>,); react { whenever $proc.stdout.lines() { $proc.kill(SIGKILL) if $++ ~~ 100 }; whenever $proc.start { } }

[18:33] <sp1983> m: my $proc = Proc::Async.new(:enc<utf8>, <cat /dev/urandom>,); react { whenever $proc.stdout.lines() { $proc.kill(SIGKILL) if $++ ~~ 100 }; whenever $proc.start { } }

[18:33] <camelia> rakudo-moar 74da84b05: OUTPUT: «A react block:␤  in block <unit> at <tmp> line 1␤␤Died because of the exception:␤    Malformed UTF-8 near bytes d3 d1␤␤»

[18:33] <sp1983> m: my $proc = Proc::Async.new(:enc<utf8-c8>, <cat /dev/urandom>,); react { whenever $proc.stdout.lines() { $proc.kill(SIGKILL) if $++ ~~ 100 }; whenever $proc.start { } }

[18:33] <camelia> rakudo-moar 74da84b05: ( no output )

[18:34] <sp1983> So looks like :enc could be the first argument as well 

[18:37] *** sp1983 left
[18:39] *** sp1983 joined
[18:39] *** sp1983 left
[18:51] *** david7832 left
[19:44] <Voldenet> Yes, you can put named arguments anywhere

[19:44] <Voldenet> m: sub foo(*@pos, :$named?) { say {:@pos, :$named} }; foo(1, 2); foo(:named, 1, 2); foo(1, 2, :named); foo(1, :named, 2)

[19:44] <camelia> rakudo-moar 74da84b05: OUTPUT: «{named => (Any), pos => [1 2]}␤{named => True, pos => [1 2]}␤{named => True, pos => [1 2]}␤{named => True, pos => [1 2]}␤»

[19:44] <Voldenet> even between positional arguments

[20:09] *** Aedil left
[20:20] *** annamalai left
[21:08] *** librasteve_ joined
[22:10] <ugexe> not before required positionals though

[22:12] <ugexe> not sure why `*@ ($arg, *@rest)` doesn't count as a required positional though

[22:12] <ugexe> m: sub foo(:$arg1, $foo) { }

[22:12] <camelia> rakudo-moar 74da84b05: OUTPUT: «===SORRY!=== Error while compiling <tmp>␤Cannot put required parameter $foo after named parameters␤at <tmp>:1␤------> sub foo(:$arg1, $foo<HERE>) { }␤    expecting any of:␤        constraint␤»

[22:13] <ugexe> m: sub foo(:$arg1, *@ ($foo, @rest)) { }

[22:13] <camelia> rakudo-moar 74da84b05: ( no output )

[23:18] *** librasteve_ left
[23:37] <SmokeMachine> m: sub a { my atomicint $a = 42; a => $a }; my :( atomicint :$a! is rw, | ) := a # is that a valid error (with LTA message) or should that work?

[23:37] <camelia> rakudo-moar 74da84b05: OUTPUT: «===SORRY!===␤Cannot bind to QAST::Var resolving to a lexicalref␤»

[23:42] <SmokeMachine> https://github.com/rakudo/rakudo/issues/6094

